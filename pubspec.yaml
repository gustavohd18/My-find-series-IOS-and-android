name: my_find_series_and_movies
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_modular: ^2.0.1
  google_fonts: ^1.1.2
  http: ^0.12.2
  rx_notifier: ^1.0.0
  intl: ^0.17.0
  youtube_player_iframe: ^1.2.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  network_image_mock: ^1.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/Tmdb-logo.png
    

